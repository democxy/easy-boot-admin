<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.democxy.modules.gen.dao.TemplateInfoDao">

	<sql id="templateInfoColumns">
		a.id AS "id",
		a.name AS "name",
		a.category AS "category",
		a.file_path AS "filePath",
		a.file_name AS "fileName",
		a.content AS "content",
		a.create_by AS "createBy",
		a.create_time AS "createTime",
		a.update_by AS "updateBy",
		a.update_time AS "updateTime",
		a.remark AS "remark",
		a.del_flag AS "delFlag"
    </sql>
    
    <sql id="templateInfoJoins">
    
    </sql>

	<select id="getById" resultType="com.democxy.modules.gen.entity.TemplateInfo">
		SELECT
		<include refid="templateInfoColumns"/>
		FROM gen_template_info a
		<include refid="templateInfoJoins"/>
		WHERE a.id = #{id}
	</select>

	<select id="findList" resultType="com.democxy.modules.gen.entity.TemplateInfo">
		SELECT
		<include refid="templateInfoColumns"/>
		FROM gen_template_info a
		<include refid="templateInfoJoins"/>
		<where>
			a.del_flag = '0'
			<if test="name != null and name != ''">
				AND a.name LIKE  concat('%',#{name},'%')
			</if>
			<if test="category != null and category != ''">
				AND a.category = #{category}
			</if>
		</where>
	</select>

	<insert id="insert">
		INSERT INTO gen_template_info(
			id,
			name,
			category,
			file_path,
			file_name,
			content,
			create_by,
			create_time,
			update_by,
			update_time,
			remark,
			del_flag
		) VALUES (
			#{id},
			#{name},
			#{category},
			#{filePath},
			#{fileName},
			#{content},
			#{createBy},
			#{createTime},
			#{updateBy},
			#{updateTime},
			#{remark},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE gen_template_info SET 	
			name = #{name},
			category = #{category},
			file_path = #{filePath},
			file_name = #{fileName},
			content = #{content},
			update_by = #{updateBy},
			update_time = #{updateTime},
			remark = #{remark}
		WHERE id = #{id}
	</update>
	
	<delete id="delete">
		delete from gen_template_info WHERE id = #{id}
	</delete>
	
</mapper>